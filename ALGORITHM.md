# AED 후보군 추출 + 지연시간 추정 알고리즘

부산 동구 초량·좌천동 산복도로 AED 최적배치의 **분석 알고리즘 전 과정**. 크게 두 알고리즘 — **① 지연시간(도달지연) 추정**, **② AED 후보군 추출(MCLP)** — 으로 나뉘며, 그 사이에 **위험도 산출**이 연결한다.

> 기준시점: 인구 2026-06 / 시설 2026-07 · 분석단위: 집계구 78개 · 좌표계 EPSG:5186(계산)·4326(표시)

> 참고: 이 문서는 **알고리즘(방법론)** 만 담습니다. 데이터셋·개인정보는 포함하지 않습니다.

---

## 0. 무엇을 "추정"하는가 (실측 vs 추정)

| 구분 | 항목 | 근거 |
|---|---|---|
| **실측** | 75세+ 인구(집계구), AED 위치·운영시간, 병원 등급, 도로망 위상, 안전센터·구급차 배치 | 공공데이터·SGIS·OSM·소방청 |
| **추정(모델)** | **도달지연**(최단경로 계산), 인구 2026-06 최신화(하이브리드 스케일), 야간 접근(운영시간 근사), 표고(SRTM 30m), 심정지 발생지(집계구 중심으로 대체) | 아래 알고리즘 |

핵심: **"어디가 위험한가(실측 고령)"** 는 데이터로, **"거기까지 얼마나 늦는가(도달지연)"** 는 그래프 최단경로로 추정한다.

---

## 1. 지연시간(도달지연) 추정 알고리즘

**목표:** 각 집계구가 가장 가까운 119안전센터에서 얼마나 떨어져 있는지(`dist_station_m`)를 도로망 최단경로로 산출 → 처치 지연 대리변수.

### 1-1. 도로망을 방향 그래프로 구성 (노트북 02)

- OSM 차량 도로망을 **방향 있는 그래프** `G=(V,E)` 로 로드(osmnx). 노드=교차로, 엣지=도로구간. 일방통행 1,890개는 단방향 엣지로 보존.
- 각 엣지에 `length`(m), `hw_type`, `width_est`, `amb_passable`(폭≥3m) 속성 부여.

### 1-2. 표고·경사 부여 (노트북 03)

- 각 노드에 SRTM 30m 표고 `elev` 부여.
- 엣지 경사(방향별 부호):

```
grade(u→v) = (elev[v] − elev[u]) / length(u→v)
```

오르막 +, 내리막 −. 표고 −4~251m, 경사 15%↑ 구간 8.9%.

### 1-3. 연결성 정리 — 강연결요소(SCC) (노트북 06)

- 구급차는 **가서(출동) + 와야(이송)** = 양방향 필요 → **강연결요소(SCC)** 개념 적용.
- 초기 SCC 61개 → **최대 SCC만 유지**(왕복 불가한 stub 72노드 제거) → **완전연결 그래프**(노드 4,756 / 엣지 12,886, SCC=1).
- 일방통행을 양방향화(강제연결)하지 않고 조각을 제거 → 방향(경사 부호) 보존.

### 1-4. 안전센터→집계구 최단경로 (Dijkstra) (노트북 10)

- 출발지 = **119안전센터 7곳**(대상지 1.5km 이내로 정제, 구급차 7/7 검증).
- 각 집계구 중심을 최근접 도로노드 `entry_node`에 **스냅**.
- 각 안전센터에서 **Dijkstra**(가중치 = `length`)로 전 노드까지 최단 도로거리 → 집계구별 최솟값.

```
S = 안전센터 7곳의 노드
for s in S:
    d_s = Dijkstra(G, source=s, weight="length")   # 모든 노드까지 최단 도로거리
best[n] = min over s in S of d_s[n]
for oa in 집계구:
    oa.dist_station_m = best[ oa.entry_node ]
```

### 1-5. 이상치 보정

- 좌천동 far-corner의 우회 7km 등 극단값이 정규화를 왜곡 → **95백분위 클립**:

```
reach_cap = min( dist_station_m, quantile(dist_station_m, 0.95) )
```

### ⚠ 1-6. 도달지연 추정의 한계 (중요)

1. **경사 미반영 편향:** Dijkstra 가중치가 `length`(도로거리)라 경사를 무시 → **가파른 고지대라도 도로상 가까우면 접근성이 좋게 과대추정**. 실제 구급차 등판 시간은 훨씬 큼.
   - 권장 보정(보류): 오르막 페널티 유효거리 `cost = length × (1 + β·max(0, 오르막경사))`, β≈6 + 민감도.
2. **시간 아님:** 현재는 거리(m). 속도·신호·도로폭 미반영(time 비용함수 후속).
3. **발생지 대체:** 실제 심정지 좌표가 아니라 집계구 중심 사용.

---

## 2. 위험도 산출 (지연시간 → 후보군 연결)

### 2-1. 인구 최신화 (하이브리드)

집계구 통계는 2024 센서스(월별 없음) → **집계구 2024 내부 분포는 유지, jumin 2026-06 동별 75+ 총량으로 스케일**:

```
p75_2026(oa) = p75_2024(oa) × ( jumin_75plus_2026(동) / Σ p75_2024 over oa in 동 )
```

동별 스케일 예: 초량2동 ×1.25, 초량3동 ×0.81, 초량6동 ×1.15. 5명미만 결측 과소집계도 이 단계에서 보정.

### 2-2. 위험도 = 발생확률 × 도달지연

```
f_age(oa)   = minmax( p75_2026 )            # 정규화 발생확률(75+)
f_reach(oa) = minmax( reach_cap )           # 정규화 도달지연
risk(oa)      = f_age × f_reach
risk_norm(oa) = minmax( risk )              # 0~1
```

- **독거 제외(A안):** AED는 **목격된** 심정지만 살림 → 독거(목격불가)는 방향이 어긋나 위험도 가중에서 제외. 별도 "조기발견 필요" 지표로 분리 예정.
- 결과: 최고위험 **초량6동(0.69)** > 좌천동 > 초량2동 > 초량1동 > 초량3동(0.09).

---

## 3. AED 후보군 추출 알고리즘 (MCLP)

**목표:** 야간에 고위험이면서 AED가 없는 집계구를 찾아, 신규 AED로 **가중 커버리지를 최대화**하는 위치를 선정.

### 3-1. 야간 AED 판정 (노트북 01·11)

```
night(aed) = (월요일 종료시각 ≥ 2200) OR (24시간 운영: 0000~2400)
```

AED 106개 중 **야간 접근 가능 60 / 주간 전용 46**.

### 3-2. 집계구 야간 커버·공백 판정

커버 반경 **R = 150m**. 집계구 중심에서 R 이내 **야간 AED**가 있으면 커버.

```
covered_night(oa) = ( 최근접 야간AED 거리 ≤ 150m )
gap(oa) = ( risk_norm 상위 30% ) AND ( NOT covered_night )
```

야간 커버 집계구 37/78(절반 이하). **공백 = 16개 집계구**(초량6동 8 최다).

### 3-3. MCLP — 그리디 최대커버리지

- 수요: `risk_norm>0` & 야간 미커버인 **41개 집계구**, 가중치 = `risk_norm`.
- 후보지: 수요 집계구 중심(실좌표 스냅은 후속).
- 알고리즘: 매 반복마다 아직 미커버 수요의 가중합을 가장 많이 새로 덮는 지점을 선택(그리디).

```
demand = { oa : risk_norm>0 AND NOT covered_night }   # 41
w(oa)  = risk_norm
selected = []
covered  = empty
while |selected| < K:                                  # K = 15
    for c in 후보지:
        gain(c) = sum( w(oa) for oa in demand if dist(oa,c)<=150 and oa not in covered )
    c* = argmax_c gain(c)
    selected += c* ;  covered += { oa : dist(oa,c*)<=150 }
```

- 결과: 신규 **15개**로 미커버 위험가중 **86% 커버**(10개 68% · 13개 79%). 배분: 초량6동 7·초량1동 3·좌천동 3·초량2동 2.
- 한계: 그리디 근사(정확해는 ILP). 후보지=집계구 중심 → 실제 설치는 24시간 접근 지점으로 스냅 필요(현장조사).

### 3-4. 후보군의 "지체(도달지연)별" 정리

각 신규 후보에 소속 집계구의 `dist_station_m`(도달지연)을 부여해 **지체 구간**으로 계층화 → 도달이 늦은 곳일수록 로컬 AED 필요가 크다.

| 구간 | 도달지연 | 의미 |
|---|---|---|
| 1 근거리 | < 300m | 안전센터 근접, 상대적 여유 |
| 2 중거리 | 300–600m | |
| 3 원거리 | 600–1000m | 골든타임 압박 |
| 4 초원거리 | ≥ 1000m | 지체 최대 — 우선 배치 |

---

## 4. 파라미터 요약

| 기호 | 값 | 의미 | 근거/민감도 |
|---|---|---|---|
| 고령 기준 | **75세+** | 발생확률 지표 | 70대→80대 급증 문턱, 65/75/80 민감도 |
| 커버 반경 R | **150 m** | AED 유효 도달 | 심정지 골든타임 도보 왕복 |
| 공백 임계 | 상위 **30%** | 고위험 정의 | 민감도 대상 |
| 클립 | **95 백분위** | 도달거리 이상치 | far-corner 우회 제거 |
| 신규 개수 K | **15** | MCLP 배치 수 | 커버곡선 86%에서 포화 |
| Dijkstra 가중 | `length` | 도달지연 | ⚠ 경사비용 미반영(보류) |
| β(권장) | 6 | 오르막 페널티 | 도입 시 민감도 4/6/8 |

---

## 5. 전체 흐름 (요약)

```
[도로망 OSM] → 방향그래프 → 표고·경사 → SCC 완전연결
                                          |
[안전센터 7] ── Dijkstra(length) ─────────┴──▶ dist_station_m (도달지연, 95pct clip)
                                                        |
[집계구 75+ 2024] ── 하이브리드 최신화 ──▶ p75_2026 ──┐  |
                                                       ├──┴─▶ risk_norm = norm(p75)×norm(reach)
[AED 106] ── 야간판정 ── 집계구 커버(R=150) ──▶ gap ──┘
                                                        |
                              위험도 × 야간공백 ── MCLP(greedy, K=15) ──▶ 신규 AED 후보 15
                                                        |
                              후보 × dist_station_m ──▶ 지체 구간(근/중/원/초원) 계층화
```

---

## 6. 재현 (노트북)

`01 병원·AED → 02 도로망 → 03 경사 → 06 연결성 → 10 집계구 위험도(도달지연) → 11 공백·MCLP`. 전 노트북 설계이유+줄별 주석.

산출물(별도 비공개 관리): `oa_risk.*`(위험도·도달지연), `oa_gap.*`(공백), 신규 AED 후보(지체구간), 통합지도(레이어 지도).
