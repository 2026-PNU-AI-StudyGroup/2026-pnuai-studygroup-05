# 실행·재현 가이드

산복도로 골든타임 프로젝트의 분석 파이프라인을 처음부터 재현하는 절차입니다.

> **요약:** ① 환경 설치 → ② `.env` 인증키 → ③ 원천 데이터 배치 → ④ 노트북 6개 순서 실행 → ⑤ 스크립트 실행

---

## 0. 사전 요구사항

| 항목 | 요구 |
|:---|:---|
| Python | **3.10 이상** |
| 디스크 | 원천 데이터 포함 약 1GB |
| 네트워크 | OSM·SRTM·공공데이터포털 API 호출 필요 |

> ⚠️ `osmnx`는 **2.x** API(`features_from_polygon`)를 사용합니다. 1.x에서는 동작하지 않습니다.

---

## 1. 환경 설치

```bash
git clone https://github.com/2026-PNU-AI-StudyGroup/2026-pnuai-studygroup-05.git
cd 2026-pnuai-studygroup-05

python -m venv .venv
source .venv/bin/activate        # Windows: .venv\Scripts\activate

pip install -r requirements.txt
```

설치 확인:
```bash
python -c "import osmnx, geopandas, networkx; print(osmnx.__version__, geopandas.__version__)"
```

---

## 2. 인증키 설정 (`.env`)

저장소에 **`.env.example` 템플릿**이 있습니다. 복사해서 값만 채우십시오.

```bash
cp .env.example .env        # Windows: copy .env.example .env
```

`.env`는 `.gitignore`에 등록되어 있어 커밋되지 않습니다.

> ⚠️ **값에 따옴표를 쓰지 마세요.** 일부 스크립트가 `line.split("=", 1)[1].strip()`로 단순 파싱하므로 `KEY="값"`으로 쓰면 따옴표까지 키에 포함되어 인증에 실패합니다.

<details>
<summary>.env 전체 형식 보기</summary>

```dotenv
# [필수] 공공데이터포털 — 반드시 "일반 인증키(Decoding)" 사용
#   ※ Encoding 키를 넣으면 이중 인코딩되어 SERVICE_KEY 오류가 납니다.
DATA_GO_KR_SERVICE_KEY=발급받은_일반인증키_Decoding

# [선택] AED 전용 키 — 비우면 위 키를 재사용
AED_SERVICE_KEY=

# [선택] NAVER Cloud Platform — Maps Directions 5 (경사계수 실측 검증용)
NCP_KEY_ID=발급받은_Client_ID
NCP_KEY=발급받은_Client_Secret

# [현재 미사용] 통계청 SGIS — archive_100m격자 노트북에서만 필요
# SGIS_CONSUMER_KEY=
# SGIS_CONSUMER_SECRET=
```

</details>

| 키 | 필요도 | 발급처 | 사용처 |
|:---|:---:|:---|:---|
| `DATA_GO_KR_SERVICE_KEY` | **필수** | [공공데이터포털](https://www.data.go.kr) 15000563 | 노트북 01(없으면 중단), 검증_119출동 |
| `AED_SERVICE_KEY` | 선택 | 공공데이터포털 15000652 | AED 위치·운영시간 (비우면 위 키 재사용) |
| `NCP_KEY_ID` / `NCP_KEY` | 선택 | [NAVER Cloud Platform](https://www.ncloud.com) Maps | 실주행 소요시간(경사계수 회귀) |
| `SGIS_CONSUMER_KEY/SECRET` | 미사용 | [통계청 SGIS](https://sgis.kostat.go.kr/developer) | archive_100m격자 노트북 전용 |

> NAVER 키가 없으면 `검증_네비게이션.py`만 건너뛰면 됩니다. 나머지 파이프라인은 정상 동작하며, 실측된 경사계수 `b_up = 0.489 s/m`가 코드에 상수로 반영되어 있습니다.

---

## 3. 원천 데이터 배치

용량과 개인정보 문제로 원천 데이터는 저장소에 포함되어 있지 않습니다(`.gitignore`). 아래 구조로 `data/`에 직접 내려받아 배치하십시오.

```
data/
├── 집계구 경계 자료/
│   └── bnd_oa_21030_2025_2Q.shp (+ .dbf .shx .prj .cpg)   # SGIS 집계구 경계, 부산 동구
├── _census_reqdoc_.../
│   └── 21030_2024년_성연령별인구.csv                        # SGIS 집계구 성·연령별 인구
├── 202606_202606_연령별인구현황_월간.csv                     # 행안부 주민등록(2026-06)
└── 소방청_전국 소방서 및 119안전센터별 구급차 정보_20250630.csv
```

| 데이터 | 취득처 |
|:---|:---|
| 집계구 경계 (2025 2Q) | 통계청 SGIS 자료제공 신청 |
| 집계구 성·연령별 인구 (2024) | 통계청 SGIS 자료제공 신청 |
| 주민등록 연령별 인구 (2026-06) | [jumin.mois.go.kr](https://jumin.mois.go.kr) |
| 119안전센터·구급차 배치 | 공공데이터포털 15113282 |

> 도로망(OSM)과 표고(SRTM)는 **코드가 API로 직접 수집**하므로 별도 배치가 필요 없습니다.

---

## 4. 분석 실행 — 노트북 6단계

**반드시 아래 순서대로** 실행하십시오. 뒤 단계는 앞 단계의 산출물(`outputs/`)을 입력으로 받습니다.

```bash
# 저장소 루트에서 실행
jupyter nbconvert --to notebook --execute --inplace code/notebooks/01_병원API_확인.ipynb
jupyter nbconvert --to notebook --execute --inplace code/notebooks/02_도로망_구축.ipynb
jupyter nbconvert --to notebook --execute --inplace code/notebooks/03_경사_부여.ipynb
jupyter nbconvert --to notebook --execute --inplace code/notebooks/06_연결성_정리.ipynb
jupyter nbconvert --to notebook --execute --inplace code/notebooks/10_집계구_조인세팅.ipynb
jupyter nbconvert --to notebook --execute --inplace code/notebooks/11_집계구_공백_MCLP.ipynb
```

Jupyter Lab에서 직접 열어 실행해도 됩니다. 모든 노트북 첫 셀에 **저장소 루트 자동 이동** 코드가 있어, `code/notebooks/`에서 열어도 `data/`·`outputs/` 상대경로가 유지됩니다.

### 단계별 입출력

| 단계 | 하는 일 | 주요 산출물 |
|:---|:---|:---|
| **01** | 응급의료기관 등급 조회(축3) + AED 수집(축4) | `outputs/aed_donggu.csv` |
| **02** | OSM 차량 도로망 → 유향 그래프 (M1) | `graph_drive_M1.graphml` |
| **03** | 노드 표고(SRTM) → 엣지 경사 부여 (M2) | `graph_drive_M2.graphml` |
| **06** | 강연결요소(SCC) 정리 → 완전연결 그래프 | `graph_drive_conn.graphml` (4,756노드/12,886엣지) |
| **10** | 집계구 경계 + 성·연령별 인구 + 도달지연 → 위험도(축1) | `oa_risk.parquet` |
| **11** | AED 야간공백(축4) + MCLP 최적배치 | `oa_gap.parquet`, `oa_mclp_new_aed.csv` |

---

## 5. 스크립트 실행

노트북 6단계가 끝난 뒤 실행합니다. 모두 **저장소 루트 기준**으로 동작하며, 어느 디렉터리에서 호출해도 자동으로 루트를 찾습니다.

```bash
# 출동시간 추정 (유효거리 → 분) + 민감도 9조합
python code/scripts/출동시간_추정.py          # → outputs/oa_delay.csv

# 경사비용(β) 도달지연·AED 후보 재산출
python code/scripts/경사비용_AED_추정.py

# 결과 지도 생성
python code/scripts/생성_통합지도.py           # → outputs/통합지도.html
python code/scripts/생성_출동시간지도.py       # → demo/출동시간지도.html
```

검증 스크립트(선택):
```bash
python code/scripts/검증_네비게이션.py         # NAVER 실주행 → 경사계수 회귀 (NCP 키 필요)
python code/scripts/검증_119출동.py            # 부산 119 출동정보 ↔ 위험도 대조
```

---

## 6. 산출물 위치

| 경로 | 내용 | git 추적 |
|:---|:---|:---:|
| `outputs/` | 그래프·위험도·공백·MCLP·지도 등 전체 산출물 | ✗ (gitignore) |
| `demo/` | 공개용 인터랙티브 지도 HTML | ✓ |
| `github_data/` | 공개용 결과 데이터셋 (집계구별 출동시간·지연) | ✓ |
| `docs/` | 알고리즘 방법론 문서 | ✓ |

---

## 7. 자주 겪는 문제

| 증상 | 원인 / 해결 |
|:---|:---|
| `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` | 공공데이터포털 **Encoding 키**를 사용함 → **Decoding 키**로 교체 |
| `AttributeError: module 'osmnx' has no attribute 'features_from_polygon'` | osmnx 1.x 설치됨 → `pip install "osmnx>=2.0"` |
| `FileNotFoundError: outputs/graph_drive_conn.graphml` | 앞 단계 노트북 미실행 → 01→02→03→06→10→11 순서 확인 |
| `FileNotFoundError: data/집계구 경계 자료/...` | 3절 원천 데이터 배치 필요 |
| 노트북에서 `data/` 경로를 못 찾음 | 첫 셀(루트 자동 이동)을 실행했는지 확인 |
| NAVER Directions 401/403 | `NCP_KEY_ID`/`NCP_KEY`가 Maps 상품에 등록되었는지 확인 |
| `FileNotFoundError: .env` | `.env` 미생성 → `cp .env.example .env` 후 값 입력 |
| 키를 넣었는데 인증 실패 | 값에 **따옴표**를 썼는지 확인 (`KEY="값"` ❌ → `KEY=값` ✅) |

---

## 8. 참고

- 알고리즘 상세: [`docs/01_데이터_전처리.md`](docs/01_데이터_전처리.md) · [`docs/02_도로기반_지연추정.md`](docs/02_도로기반_지연추정.md) · [`docs/03_AED_후보군_추정.md`](docs/03_AED_후보군_추정.md) · [`docs/05_출동시간_추정.md`](docs/05_출동시간_추정.md)
- 데이터 출처·라이선스: [`LICENSE`](LICENSE), [README 8절](README.md#8-참고문헌-및-출처)
